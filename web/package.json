{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "npm --prefix ../packages/lonk-client run build",
    "dev": "vite",
    "prebuild": "npm --prefix ../packages/lonk-client run build",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": { "lonk-client": "*" },
  "devDependencies": { "typescript": "~5.6.0", "vite": "^6.0.0" }
}
